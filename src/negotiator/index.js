
/**
* @description returns tokens based on the filter criteria
*/

export class Negotiator {
  
  /**
  * @param {Array} filter optional token rules
  */
  constructor(filter) {
    this.filter = filter;
  }

  /**
  * @returns {object} token data 
  */
  async getTokenInstances(){}
  
}

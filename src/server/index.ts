// import { ethers } from "ethers";

export class Server {

  // constructor() {};

  // async recoverAddress (signature:any, originalMsg:string) {
    
  //   const msgHash = ethers.utils.hashMessage(originalMsg);

  //   const msgHashBytes = ethers.utils.arrayify(msgHash);

  //   return ethers.utils.recoverAddress(msgHashBytes, signature);

  // }

  // async resolveNFTTokenOwnership (signature:any, originalMsg:string, nfts:any) {

    // const signatureAddress = await this.recoverAddress(signature, originalMsg);

    // nfts - chain location, nft data

    // x.map((nft) => {  });

    // if index of false (no nft found) fail the check

  // }

}
import { AuthenticationMethod, AuthenticationResult, AbstractAuthentication } from "../auth/abstractAuthentication";

describe('auth spec', () => {
	test('client/auth test', () => {
		console.log("started..");
	});
});

